The user asked a question rather than requesting implementation. Produce a user-facing answer report — not a task plan or implementation checklist.

**Actions:**
1. Restate the question in one sentence.
2. Answer directly using investigation already done in the plan step when available; run Read/Grep/Glob only if a fact is still missing.
3. Cite concrete evidence (file paths, config keys, runtime values) when the answer depends on the codebase or environment.
4. Keep scope to what was asked; do not propose implementation work unless the user explicitly requested it.
5. If the question cannot be answered with available information, state what is missing and stop — do not invent facts.
